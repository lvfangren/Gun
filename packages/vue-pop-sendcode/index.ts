import VuePopSendCode from './index.vue';
type VuePopSendCodeType = {
  name?: string
}
const instance = Object.assign({}, VuePopSendCode, {
    install(app, options?: VuePopSendCodeType) {

    }
})

export default instance;
